# Editor integration

`web-angular` owns the Angular application shell. `editor-react` owns the React implementation. Framework-neutral types belong to `editor-contract`. `feature-host` is the integration layer that mounts the React editor for the Angular shell.
